{
  "name": "query-object-mapper",
  "version": "1.0.1",
  "author": "Karel Horský",
  "description": "Helper for mapping vue's URL query to parameter object",
  "license": "MIT",
  "private": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/aipng/query-object-mapper"
  },
  "bugs": {
    "url": "https://github.com/aipng/query-object-mapper/issues"
  },
  "homepage": "https://github.com/aipng/query-object-mapper#readme",
  "scripts": {
    "build": "tsc",
    "test": "jest tests",
    "lint": "eslint",
    "prepare": "rm -rf dist && npm run build",
    "prepublishOnly": "npm test && npm run lint",
    "preversion": "npm run lint",
    "version": "git add -A src",
    "postversion": "git push && git push --tags"
  },
  "keywords": [
    "query",
    "object",
    "mapper"
  ],
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "eslint": "^8.5.0",
    "jest": "^27.4.5",
    "ts-jest": "^27.1.2",
    "typescript": "^4.5.4"
  }
}
